---
output: github_document
---

# targets-iSSA

```{r, results = "asis", echo = FALSE}
library(targets)
cat(c("```mermaid", tar_mermaid(TRUE, reporter = 'silent'), "```"), sep = "\n")
```

## R setup

```{r, eval = FALSE}
renv::restore()
```


## Run the workflow

```{r, eval = FALSE, echo = TRUE}
library(targets)
tar_make()
```


