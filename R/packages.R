# Packages ----------------------------------------------------------------
library(targets)
library(tarchetypes)
library(qs)
library(visNetwork)

library(renv)
library(conflicted)
conflicts_prefer(raster::extract)

library(data.table)

library(amt)
library(sf)
library(raster)

library(parsedate)

library(ggplot2)
library(ggthemes)

library(distanceto)

library(glmmTMB)
require(broom.mixed)
require(tidyr)
require(dplyr)
require(tibble)
